# Validated data (local, ignored)

This directory holds outputs from validation, correction, re-validation, and
deduplication. A file in this directory is still not final training data until
`build_datasets.py` verifies its quality gate and split isolation.
