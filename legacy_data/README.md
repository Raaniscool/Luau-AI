# Legacy-data archive

The repository had no prior datasets when the Phase 1 pipeline was introduced
(2026-09-19). Preserve any future compatible pre-pipeline material here with a manifest
rather than deleting it or silently blending it into the curated corpus.
