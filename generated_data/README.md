# Generated data (local, ignored)

This is stage 1 output from `python -m scripts.generate_examples`. It is intentionally
ignored by Git because it can be large and must be reviewed before it becomes training
data. Files here are not eligible for fine-tuning.
